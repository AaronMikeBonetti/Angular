import { ExponentialPowerPipe } from './exponential-power.pipe';

describe('ExponentialPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponentialPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
